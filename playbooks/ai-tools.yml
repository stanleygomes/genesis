# @sub-options: antigravity-cli, github-copilot, hermes-agent, claude-code, open-claw
---
- name: AI tools setup
  hosts: localhost
  connection: local
  become: true
  gather_facts: false

  pre_tasks:
    - name: Gather facts as normal user to set ansible_env correctly
      setup:
      become: false

  roles:
    - { role: antigravity-cli, when: install_antigravity_cli | default(false) | bool }
    - { role: github-copilot, when: install_github_copilot | default(false) | bool }
    - { role: hermes-agent, when: install_hermes_agent | default(false) | bool }
    - { role: claude-code, when: install_claude_code | default(false) | bool }
    - { role: open-claw, when: install_open_claw | default(false) | bool }
