---
- name: Desktop Setup
  hosts: localhost
  connection: local
  become: true
  gather_facts: false

  pre_tasks:
    - name: Gather facts as normal user to set ansible_env correctly
      setup:
      become: false
  roles:
    - google-chrome
    - gnome-setup
    - fonts
    # - mongodb-compass
    - dbeaver
    - sdkman
    - pnpm
    # - harlequin
    - python-uv
    - php
    - go
    # - lazysql
    # - ollama
    - antigravity
    - postman
    # - bruno
    - lazygit
    - lazydocker
    - posting
    - filezilla
    - desktop-entries
