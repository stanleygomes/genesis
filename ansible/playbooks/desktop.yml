---
- name: My Custom Desktop Setup
  hosts: localhost
  connection: local
  become: true
  gather_facts: false

  pre_tasks:
    - name: Gather facts as normal user to set ansible_env correctly
      setup:
      become: false

    - name: Update apt cache (ignoring unreachable repos)
      shell: apt-get update || true
      become: true
      changed_when: false
  roles:
    - system
    - google-chrome
    - dbeaver
    - java
    - python
    - php
    - go
    - antigravity
    - vscode
    - lazydocker
    - antigravity-cli
    - github-copilot
    - ghostty
    - terraform

