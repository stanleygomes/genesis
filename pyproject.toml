[project]
name = "genesis"
version = "0.1.0"
description = "Genesis CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.12.3",
    "inquirerpy>=0.3.4",
]

[tool.uv]
managed = true
